# procs:
#   inngest:
#     shell: "npm runn inngest:dev"
#   next:
#     shell: "npm run dev"

# # 3:21

procs:
  inngest:
    cmd: ["cmd.exe", "/c", "npm", "run", "inngest:dev"]

  next:
    cmd: ["cmd.exe", "/c", "npm", "run", "dev"]
